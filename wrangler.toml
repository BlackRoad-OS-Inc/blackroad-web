name = "blackroad-web"
compatibility_date = "2024-01-01"
pages_build_output_dir = "out"

# Stripe env vars are set via Cloudflare dashboard:
# Settings > Environment variables > Production
# Required: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, BASE_URL
